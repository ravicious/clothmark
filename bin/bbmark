#!/usr/bin/env ruby

require 'rubygems'
require File.expand_path(File.dirname(__FILE__) + '/../lib/clothmark')

input = ARGV[0]
output = ARGV[1] || nil

@bbmark = BBMark.new(input, output)
@bbmark.convert
@bbmark.save_to_file
